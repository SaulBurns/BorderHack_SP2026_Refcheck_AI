fastapi
uvicorn
python-multipart
python-dotenv
certifi

# Optional: YOLO object detection (DETECTOR=yolov8 or hybrid). Upgraded to the
# latest stable Ultralytics (YOLO26 flagship) in Sprint 18A. Heavy (pulls in torch);
# imported lazily and not required for the default DETECTOR=claude_vision. Install
# only when running YOLO-based detection. The default weights are yolo26n.pt
# (override with YOLO_MODEL, e.g. yolo11n.pt / yolov8n.pt).
# ultralytics>=8.4.104,<8.5

# Google Gemini provider (AI_PROVIDER=gemini, or a Gemini leg of AI_PROVIDER=router).
# Now a first-class dependency (Sprint 17C) so Gemini is production-deployable out of
# the box — the image can run Gemini without a custom build. Still imported lazily, so
# mock/anthropic deploys are unaffected and a missing SDK degrades gracefully rather
# than crashing (surfaced by /api/health/ready and startup validation).
google-genai

# NBA game-metadata enrichment (basketball `game_context`).
# Pure-Python but pulls in requests + pandas. Imported lazily and fully guarded:
# the app still runs and returns verdicts if it is missing or rate-limited
# (game_context degrades to "unresolved"/"skipped"/"unavailable").
nba_api
