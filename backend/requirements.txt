fastapi
uvicorn
python-multipart
python-dotenv
certifi

# Optional: YOLOv8 object detection (DETECTOR=yolov8 or hybrid).
# Heavy (pulls in torch); imported lazily and not required for the default
# DETECTOR=claude_vision. Install only when running YOLO-based detection.
# ultralytics

# NBA game-metadata enrichment (basketball `game_context`).
# Pure-Python but pulls in requests + pandas. Imported lazily and fully guarded:
# the app still runs and returns verdicts if it is missing or rate-limited
# (game_context degrades to "unresolved"/"skipped"/"unavailable").
nba_api
